This repository does not currently define any tracked project data directories or persistent output locations. Add entries here when `data/` or `outputs/` are introduced.

Operational references:
- `docs/reboot-startup.md` - installation and verification notes for the reboot startup LaunchAgent.
